import type { SceneState } from "../types/scene_state";

export const step23 = {
  title: "Dot Product and Projection",
  description: `Understanding how to find this projection is the crucial first step in more complex calculations, like determining if two convex polygons overlap. For example, take a look at the Separating Axis Theorem, you should now know the essentials.`,
  reduceState: (sceneState: SceneState): SceneState => {
    return sceneState;
  },
  updateState: (sceneState: SceneState): SceneState => {
    return sceneState;
  },
};
